# 三条路径测试记录(2026-09-11)

> 对应 `docs/需求说明.md` 的测试要求与设计文档第 4 节。每条路径记录:复现步骤 / 预期 / 实际 / 结论。填写时如实记录,不通过的写清现象与后续处理。

## 路径一:正常完成

- 步骤:登记研究员密钥 → 输入"我又熬夜刷手机到凌晨三点" → 点击「开始吐槽」
- 预期:生成含「内部批注」的实时报告;观察计数 +1;连续 5 次后解锁「闲着无聊」;刷新页面进度保留
- 实际:(待填写)
- 结论:(通过 / 不通过)

## 路径二:空输入 / 缺内容

- 步骤 2a:输入框留空,点击「开始吐槽」
- 预期:状态区提示"观察样本为空…",不消耗观察次数
- 实际:(待填写)
- 步骤 2b:尝试输入超过 200 字
- 预期:输入框 maxlength 截断,计数显示 200 / 200
- 实际:(待填写)
- 结论:(通过 / 不通过)

## 路径三:失败与恢复

- 步骤 3a(密钥无效):登记一个错误密钥 → 提交样本
- 预期:提示"密钥未通过验证…",引导卡重新出现,可重新登记
- 实际:(待填写)
- 步骤 3b(网络/服务失败):断网后提交样本(或由开发者临时把 API_MODEL 改为不存在的模型名)
- 预期:提示"研究员通讯中断/无法归档";连续 2 次失败后引导卡给出「先翻离线档案」;点击后能调阅历史样本
- 实际:(待填写)
- 步骤 3c(恢复):恢复网络(或改回正确模型名)→ 登记正确密钥重试
- 预期:回到实时生成,报告正常
- 实际:(待填写)
- 结论:(通过 / 不通过)

## 自动化测试(补充)

- 命令:`node --test`(需 Node ≥ 18)
- 范围:prompt.js 输出契约校验(11 例)、api-client.js 错误分类与存储降级(6 例)
- 结果:2026-09-10 开发机 17/17 通过(提交 13058ec 时点)
